#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    // faster input and output
    ios::sync_with_stdio(0);
    cin.tie(0);

    int cases;
    cin >> cases;
    for (int t = 1; t <= cases; t++) {

    }
    return 0;
}
